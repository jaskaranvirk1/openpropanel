# Build output (anchored to repo root so they never match source dirs like
# cmd/openpropanel/)
/bin/
/dist/
/openpropanel
/openpropanel.exe

# Local runtime state (dev fallback)
/data/

# Tailwind toolchain
/node_modules/
/tailwindcss
/tailwindcss.exe

# Editor / OS
.DS_Store
*.log
